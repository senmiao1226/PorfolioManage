spring:
  datasource:
    url: jdbc:h2:mem:portfoliotest;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  cors:
    allowed-origins: "http://localhost:5173"
  pricing:
    cached-price-base: "https://example.com"
